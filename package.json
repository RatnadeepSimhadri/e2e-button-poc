{
  "name": "zoid-cross-window",
  "version": "1.0.0",
  "description": "Cross window application using zoid",
  "main": "server.js",
  "scripts": {
    "start": "npm run build && nodemon index.js",
    "build": "vite build",
    "clean": "rm -rf dist node_modules && npm i"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ejs": "^3.1.9",
    "zoid": "^9.0.86"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "vite": "^5.0.0"
  }
}